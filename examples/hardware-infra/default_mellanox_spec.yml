---
type: hardware-infra-spec # Defines machine types and other info but does not provision them
name: mellanox_packet
provider: packet
hardware_infra_spec: 
  master_type: c1.small
  worker_type: m2.xlarge
